services:
  nginx:
    container_name: nginx
    build: ./requirements/nginx
    image: nginx
    pull_policy: never
    restart: unless-stopped
    env_file:
      - .env
    ports: 
      - "443:443"
    networks: 
      - webnet
    volumes:
      - webdata:/var/www/html
    depends_on:
      - wordpress
  
  wordpress:
    container_name: wordpress
    build: ./requirements/wordpress
    image: wordpress
    pull_policy: never
    restart: unless-stopped
    secrets:
      - wp_admin_password
      - wp_user_password
      - db_password
    env_file:
      - .env
    networks: 
      - webnet
    volumes:
      - webdata:/var/www/html
    depends_on:
      - mariadb
  
  mariadb:
    container_name: mariadb
    build: ./requirements/mariadb
    image: mariadb
    pull_policy: never
    restart: unless-stopped
    secrets:
      - db_password
      - db_root_password
    env_file:
      - .env
    networks:
      - webnet
    volumes:
      - dbdata:/var/lib/mysql

networks:
  webnet:
    name: webnet

volumes:
  webdata:
    name: webdata
    driver_opts:
      type: none
      device: /home/eandela/data/webdata
      o: bind
  dbdata:
    name: dbdata
    driver_opts:
      type: none
      device: /home/eandela/data/dbdata
      o: bind

secrets:
  db_password:
    file: ../.secrets/db_password.txt
  db_root_password:
    file: ../.secrets/db_root_password.txt
  wp_admin_password:
    file: ../.secrets/wp_admin_password.txt
  wp_user_password:
    file: ../.secrets/wp_user_password.txt